import React from 'react';
import ReactDOM from 'react-dom';
import './index.css'; // Optional if you don't have this file
import App from './App';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);

